# Learn more about configuring your app at https://shopify.dev/docs/apps/tools/cli/configuration

client_id = "95e18de12e9140ab572fde0b0604aff2"
name = "Manca: Quote Analytics"
application_url = "https://manca-quote-pipeline.onrender.com/"
embedded = true

[sidekick]
extensions_summary = "Search and analyze draft-order quote pipeline data: count open quotes, find stale quotes with no customer response for over 7 days, list quotes deleted by the customer, and report the monthly quote-to-order conversion rate."

[access_scopes]
# Learn more at https://shopify.dev/docs/apps/tools/cli/configuration#access_scopes
scopes = "read_draft_orders"

[webhooks]
api_version = "2026-10"

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/uninstalled"
  topics = [ "app/uninstalled" ]

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/scopes_update"
  topics = [ "app/scopes_update" ]

  [[webhooks.subscriptions]]
  topics = [ "draft_orders/create", "draft_orders/update", "draft_orders/delete" ]
  uri = "/webhooks/draft-orders"
  include_fields = [ "id", "status", "total_price", "currency", "created_at", "updated_at", "completed_at", "invoice_sent_at" ]

[auth]
redirect_urls = [ "https://manca-quote-pipeline.onrender.com/auth/callback" ]

[build]
automatically_update_urls_on_dev = false
